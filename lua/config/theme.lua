require('rose-pine').setup({
    variant = 'moon',
    dim_inactive = true,
    extend_background_behind_borders = true,
    enable = {
        terminal = true,
    },
})

vim.cmd.colorscheme('rose-pine')
